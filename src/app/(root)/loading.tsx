import Loader from "@/components/Loader"

const Loading = () => {
    return (
        <div className="w-full h-full">
            <Loader variant="purple" fullScreen />
        </div>
    )
}

export default Loading